version: '3.8'

services:
  valkey:
    image: docker.io/bitnami/valkey:latest
    container_name: valkey_standalone
    restart: always
    environment:
      - VALKEY_PASSWORD=bitnami
    ports:
      - "6379:6379"
    volumes:
      - valkey_data:/bitnami/valkey/data

volumes:
  valkey_data:
    driver: local
