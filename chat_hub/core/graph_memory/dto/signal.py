class Signal:
    def __init__(self, content, vector, bitmask, vector_id):
        self.content = content
        self.vector = vector
        self.bitmask = bitmask
        self.vector_id = vector_id
